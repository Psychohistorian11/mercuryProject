export interface SongFirstContent{
    id: string
    name: string;
    time: string;
   datePublished: string;
}

export interface SongSecondContent{
    id: string
    file: File,
    image: File,
}